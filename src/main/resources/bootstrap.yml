spring:
  application:
    name: turbine
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: test
      username: user
server:
  port: 8989

management:
  port: 8990